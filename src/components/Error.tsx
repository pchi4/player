import {
  Text,
  Box,
  HStack,
  Image,
  VStack,
  Progress,
  Center,
} from "native-base";
import { Dimensions, TouchableOpacity, Platform } from "react-native";

export const Error = () => {
  return (
    <Box>
      <Center>
        <Text>Error Page</Text>
      </Center>
    </Box>
  );
};
